version: 2.1
orbs:
  node: circleci/node@3.0.0
jobs:
  run-tests:
    docker:
      - image: "cimg/base:stable"
    steps:
      - node/test:
          run-command: npm run bootstrap && npm t
workflows:
  test:
    jobs:
      - run-tests
