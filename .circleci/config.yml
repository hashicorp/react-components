version: 2.1
orbs:
  node: circleci/node@3.0.0
jobs:
  test:
    executor:
      name: node/default
      tag: "13"
    steps:
      - checkout
      - run:
          command: npm run bootstrap
      - run:
        command: npm t
workflows:
  test:
    jobs:
      - test
