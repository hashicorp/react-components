---
componentName: 'ProductBadge'
---

<LiveComponent>
  {`
    <section>
      <div style={{display: 'flex', flexWrap: 'wrap', gap: 16, backgroundColor: 'black', padding: '16px'}}>
        <ProductBadge
          productName={'waypoint'}
          appearance={'dark'}
          hasDot={true}
          variant={'secondary'}
        />
      </div>
      <div style={{display: 'flex', flexWrap: 'wrap', gap: 16, backgroundColor: 'white', padding: '16px'}}>
        <ProductBadge
          productName={'waypoint'}
          appearance={'light'}
          hasDot={true}
          variant={'secondary'}
        />
      </div>
    </section>

  `}
</LiveComponent>

### Props

<PropsTable props={componentProps} />

### Examples

<section>
  <div>
    <h2>Dark</h2>
    <div>
      <h3>Primary</h3>
      <div style={{display: 'flex', flexWrap: 'wrap', gap: 16, backgroundColor: 'black', padding: '16px'}}>
        {[
          'terraform',
          'vault',
          'consul',
          'nomad',
          'waypoint',
          'boundary',
          'packer',
          'vagrant',
        ].map((product, i) => {
          return (
            <ProductBadge
              key={i}
              productName={product}
              appearance={'dark'}
              variant={'primary'}
            />
          )
        })}
      </div>
    </div>
    <div>
      <h3>Secondary</h3>
      <div style={{display: 'flex', flexWrap: 'wrap', gap: 16, backgroundColor: 'black', padding: '16px'}}>
        {[
          'terraform',
          'vault',
          'consul',
          'nomad',
          'waypoint',
          'boundary',
          'packer',
          'vagrant',
        ].map((product, i) => {
          return (
            <ProductBadge
              key={i}
              productName={product}
              appearance={'dark'}
              variant={'secondary'}
            />
          )
        })}
      </div>
    </div>
    <div>
      <h3>hasDot</h3>
      <div style={{display: 'flex', flexWrap: 'wrap', gap: 16, backgroundColor: 'black', padding: '16px'}}>
        {[
          'terraform',
          'vault',
          'consul',
          'nomad',
          'waypoint',
          'boundary',
          'packer',
          'vagrant',
        ].map((product, i) => {
          return (
            <ProductBadge
              key={i}
              productName={product}
              appearance={'dark'}
              variant={'secondary'}
              hasDot={true}
            />
          )
        })}
      </div>
    </div>
  </div>
  <div>
    <h2>Light</h2>
    <div>
      <h3>Primary</h3>
      <div style={{display: 'flex', flexWrap: 'wrap', gap: 16, backgroundColor: 'white', padding: '16px'}}>
        {[
          'terraform',
          'vault',
          'consul',
          'nomad',
          'waypoint',
          'boundary',
          'packer',
          'vagrant',
        ].map((product, i) => {
          return (
            <ProductBadge
              key={i}
              productName={product}
              appearance={'light'}
              variant={'primary'}
            />
          )
        })}
      </div>
    </div>
    <div>
      <h3>Secondary</h3>
      <div style={{display: 'flex', flexWrap: 'wrap', gap: 16, backgroundColor: 'white', padding: '16px'}}>
        {[
          'terraform',
          'vault',
          'consul',
          'nomad',
          'waypoint',
          'boundary',
          'packer',
          'vagrant',
        ].map((product, i) => {
          return (
            <ProductBadge
              key={i}
              productName={product}
              appearance={'light'}
              variant={'secondary'}
            />
          )
        })}
      </div>
    </div>
    <div>
      <h3>hasDot</h3>
      <div style={{display: 'flex', flexWrap: 'wrap', gap: 16, backgroundColor: 'white', padding: '16px'}}>
        {[
          'terraform',
          'vault',
          'consul',
          'nomad',
          'waypoint',
          'boundary',
          'packer',
          'vagrant',
        ].map((product, i) => {
          return (
            <ProductBadge
              key={i}
              productName={product}
              appearance={'light'}
              variant={'secondary'}
              hasDot={true}
            />
          )
        })}
      </div>
    </div>
  </div>
</section>
