import services from '../services'

export default {
  segmentServices: [
    services.googleAnalytics,
    services.googleTagManager,
    services.linkedIn,
    services.heap,
    services.marketo,
    services.googleAds,
  ],
}
