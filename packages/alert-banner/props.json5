{
  tag: {
    type: 'string',
    description: 'The text contained inside the tag to the left',
    required: true,
    control: 'text',
  },
  text: {
    type: 'string',
    description: 'The text in the main area',
    required: true,
    control: 'text',
  },
  url: {
    type: 'string',
    description: 'where to link to when clicked',
    required: true,
    control: 'text',
  },
  theme: {
    type: 'string',
    description: 'Color theme to match a specific product',
    control: 'select',
    options: ['consul', 'terraform', 'nomad', 'vault', 'red', 'blue'],
  },
  linkText: {
    type: 'string',
    description: 'Secondary text styled as a link',
    control: 'text',
  },
  name: {
    type: 'string',
    description: 'string used for cookie name (defaults to slugified text property)',
    control: 'text',
  },
  expirationDate: {
    type: 'string',
    description: 'A datetime string that, when set, controls if the alert banner should appear',
    control: 'text',
  },
}
