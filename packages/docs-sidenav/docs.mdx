---
componentName: 'DocsSidenav'
---

DocsSidenav renders a tree of links, with the option to include nested sections. Horizontal dividers can also be included between items.

<LiveComponent>{`<DocsSidenav
  product="${componentProps.product.testValue}"
  currentPath="${componentProps.currentPath.testValue}"
  baseRoute="${componentProps.baseRoute.testValue}"
  navData={${JSON.stringify(componentProps.navData.testValue, null, 2)}}
/>`}</LiveComponent>

<UsageDetails packageJson={packageJson} />

### Props

<PropsTable props={componentProps} />
