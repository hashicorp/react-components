---
componentName: 'DocsSidenav'
---

This is a cool component for documentation

<LiveComponent>{`<DocsSidenav
  product="${componentProps.product.testValue}"
  currentPage="${componentProps.currentPage.testValue}"
  category="${componentProps.category.testValue}"
  order={${JSON.stringify(componentProps.order.testValue, null, 2)}}
  data={${JSON.stringify(componentProps.data.testValue, null, 2)}}
/>`}</LiveComponent>

<UsageDetails packageJson={packageJson} />
