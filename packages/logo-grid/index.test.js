// import LogoGrid from './'

test.todo('Displays logos that are passed in via the `data` prop')
test.todo(
  'Displays a tooltip when clicked for logos if the `details` prop is true and the logo has a `dewcription`, and closes the tooltip on "X"'
)
test.todo(
  'Adds a hash to the URL when a tooltip is opened if the `hashUrl` prop is true'
)
test.todo(
  'Renders the `size` prop as a class on the root, defaults to `small`, but accepts `medium` or `large`'
)
test.todo(
  'Adds a `no-borders` class if the `removeBorders` prop is set to true'
)
test.todo(
  'renders the correct color logo based on the `color` prop for `white` or `monochrome`'
)
test.todo(
  'links the logo to its respective integration page if the `integrationLink` prop is used'
)
