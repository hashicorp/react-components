---
componentName: 'Badge'
---

Badges are non-interactive labels highlighting metadata such as status or categorization.

<LiveComponent>
  {`
<>
  <div style={{ display: 'flex', gap: 16 }}>
    <Badge variant="primary" theme="action" page="light">New</Badge>
    <Badge variant="primary" theme="neutral" page="light">New</Badge>
    <Badge variant="primary" theme="nomad" page="light">New</Badge>
  </div>
</>
`}
</LiveComponent>

<PropsTable props={componentProps} />

## Examples

<div style={{display: 'grid', gridTemplateRows: '1fr', gap: '32px'}}>
  {
    [
      'action',
      'neutral',
      'boundary',
      'consul',
      'nomad',
      'packer',
      'terraform',
      'vagrant',
      'vault',
      'waypoint',
    ].map((t) => {
      return (
        <div style={{display: 'grid', gridTemplateColumns: 'repeat(6, max-content)', gap: '8px'}}>
          {
            [
              'light',
              'faint',
              'strong',
              'strongFaint',
              'action',
              'actionFaint'
            ].map((p) => {
              return ( 
                <div style={{display: 'grid', gridTemplateRows: '1fr 1fr', gap: '16px'}}>
                  {
                    [
                      'primary',
                      'secondary',
                    ].map((v) => {
                      return (
                        <Badge variant={v} theme={t} page={p} />
                      )
                    })
                  }  
                </div>
              )
            })
          }
        </div>
      )
    })
  }
</div>
